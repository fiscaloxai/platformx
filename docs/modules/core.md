# `platformx.core`

Purpose
- High-level orchestrator for the Platform: coordinates datasets, registries, and subsystems.

Key classes / functions
- `Platform`: register datasets, adapters, and expose subsystems such as retrieval and audit.

Safety features
- Registration APIs require provenance metadata and schema validation to ensure traceability.

Usage
- See `docs/getting_started.md` for a quick example.
